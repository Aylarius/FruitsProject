{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un fruit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" class="btn btn-default btn-add" />
    {{ form_end(form) }}
    <br><br>

    <a href="{{ path('fruits_index') }}" class="btn btn-default">Retour à la liste</a>
{% endblock %}
